name: KirinDL Sponsor Info

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest

    steps:
      - name: Sponsor workflow status
        run: |
          echo "KirinDL does not automatically rewrite README sponsor data."
          echo "Upstream project credits and sponsor links remain documented manually."
          echo "No PAT, GitHub Pages deployment, or automatic README mutation is used."
